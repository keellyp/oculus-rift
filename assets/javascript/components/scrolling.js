import ScrollReveal from 'scrollreveal'
window.sr = ScrollReveal()

const reveal = {}
reveal.top = {
  scale: 1,
  distance: '50px',
}
reveal.left = {
  scale: 1,
  distance: '100%',
  opacity: 100,
  origin: 'left'
}
reveal.right = {
  scale: 1,
  distance: '100%',
  opacity: 100,
  origin: 'right'
}
reveal.opacity = {
  scale: 1,
  opacity: 0,
  distance: 'Opx'
}

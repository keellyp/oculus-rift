import ScrollReveal from 'scrollreveal'
window.sr = ScrollReveal()

const reveal = {}
reveal.top = {
  scale: 1,
  distance: '20rem',
  origin: 'bottom',
  viewFactor: 0.5,
  reset: true
}
reveal.left = {
  scale: 1,
  distance: '100%',
  opacity: 100,
  origin: 'left'
}
reveal.right = {
  scale: 1,
  distance: '100%',
  opacity: 100,
  origin: 'right'
}
reveal.opacity = {
  scale: 1,
  opacity: 0,
  distance: 'Opx'
}
